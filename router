//
//
pragma solidity ^0.8.16

contract router { 
  funciton addLiquidity(
    address tokenA
    address tokenB
    units amountADesired,
    unitsBDesired,
    unit ammountAMin,
    unit ammountBMin,
    address to,
    unit deadline
    ) external returns(unit ammountA, unitammountB, unit liquidity) {}
  }
  
